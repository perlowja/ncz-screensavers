/* AUTO-GENERATED by scripts/gen_help.py - do not edit.
 * Per-saver option table for the SDL shell's --help. */

extern const char* rss_saver_name;
extern const char* rss_saver_options;

const char* rss_saver_name = "euphoria";
const char* rss_saver_options =
    "  -default        <1..9>  (presets)\n"
    "  -wisps          <0..100>\n"
    "  -background     <0..100>\n"
    "  -density        <1..100>\n"
    "  -visibility     <1..100>\n"
    "  -speed          <1..100>\n"
    "  -feedback       <0..100>\n"
    "  -feedbackspeed  <1..100>\n"
    "  -feedbacksize   <FEEDBACKSIZE_MIN..FEEDBACKSIZE_MAX>\n"
    "  -wireframe      <0..1>\n"
    "  -texture        <0..4>\n"
    ;

/* ---- live-option runtime (web settings panel; see shell.html) ---- */
#include <string.h>

extern int dBackground;
extern int dDensity;
extern int dFeedback;
extern int dFeedbacksize;
extern int dFeedbackspeed;
extern int dSpeed;
extern int dTexture;
extern int dVisibility;
extern int dWireframe;
extern int dWisps;

static struct { const char* name; int* addr; int staged; int dirty; } rss_opts[] = {
    { "wisps", &dWisps, 0, 0 },
    { "background", &dBackground, 0, 0 },
    { "density", &dDensity, 0, 0 },
    { "visibility", &dVisibility, 0, 0 },
    { "speed", &dSpeed, 0, 0 },
    { "feedback", &dFeedback, 0, 0 },
    { "feedbackspeed", &dFeedbackspeed, 0, 0 },
    { "feedbacksize", &dFeedbacksize, 0, 0 },
    { "wireframe", &dWireframe, 0, 0 },
    { "texture", &dTexture, 0, 0 },
};

extern "C" int rss_set_option(const char* name, int value) {
    for (unsigned i = 0; i < sizeof(rss_opts)/sizeof(rss_opts[0]); ++i) {
        if (!strcmp(rss_opts[i].name, name)) {
            rss_opts[i].staged = value;
            rss_opts[i].dirty = 1;
            return 1;
        }
    }
    return 0;
}

extern void initSaver();
extern void cleanUp();
extern "C" void rss_restart(void) {
    cleanUp();  // frees using the OLD option values
    for (unsigned i = 0; i < sizeof(rss_opts)/sizeof(rss_opts[0]); ++i) {
        if (rss_opts[i].dirty) {
            *rss_opts[i].addr = rss_opts[i].staged;
            rss_opts[i].dirty = 0;
        }
    }
    initSaver();  // allocates using the NEW option values
}
